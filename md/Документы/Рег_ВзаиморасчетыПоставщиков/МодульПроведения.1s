// ********************
Процедура ОбработкаПроведения()   
	сч = 0;
	ВыбратьСтроки();
	Пока ПолучитьСтроку() = 1 Цикл   
		состояние("Строка "+сч);
		Регистр.ВзаиморасчетыПоставщиков.ПривязыватьСтроку(НомерСтроки);	 
		Регистр.ВзаиморасчетыПоставщиков.ДвижениеПриход(Фирма1,Контрагент,Договор,СтавкаНДС,КредДокумент,Долг,,,);
		сч  = сч+1;
	КонецЦикла;
КонецПроцедуры
