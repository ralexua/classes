// ********************
Процедура ОбработкаПроведения() 
	сч = 0;
	ВыбратьСтроки();
	Пока ПолучитьСтроку() = 1 Цикл  
		состояние("Строка "+сч);
		Регистр.ВзаиморасчетыПокупателей.ПривязыватьСтроку(НомерСтроки);	 
		Регистр.ВзаиморасчетыПокупателей.ДвижениеПриход(Фирма1,Контрагент,Договор,СтавкаНДС,КредДокумент,Долг,,,);
		сч  = сч+1;
	КонецЦикла;
КонецПроцедуры
